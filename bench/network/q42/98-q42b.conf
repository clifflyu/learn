# Q42 实测用的第二个 vhost：反向代理到 9099，那边由一个故意说胡话的
# python 服务占着，用来制造第三种 502 —— "upstream sent invalid header"。
server {
    listen 8082;
    server_name _;

    error_log /docker.stderr warn;

    location / {
        proxy_pass http://127.0.0.1:9099;
        proxy_read_timeout 3s;
    }
}
