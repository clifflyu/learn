# PHP 高级工程师面试知识体系

> 用途：高级 PHP 后端岗位面试复习、查漏补缺与自测。  
> 建议基线：以 PHP 8.x 为主，兼顾存量 PHP 7 项目的迁移经验。不要只背结论，要能讲清原理、使用场景、取舍、故障案例和数据结果。

## 目录

1. [高级工程师的能力模型](#1-高级工程师的能力模型)
2. [PHP 语言基础与底层原理](#2-php-语言基础与底层原理)
3. [面向对象、设计原则与设计模式](#3-面向对象设计原则与设计模式)
4. [Composer 与 PHP 工程生态](#4-composer-与-php-工程生态)
5. [Web、HTTP 与网络基础](#5-webhttp-与网络基础)
6. [主流框架与应用设计](#6-主流框架与应用设计)
7. [MySQL 与数据库](#7-mysql-与数据库)
8. [Redis 与缓存体系](#8-redis-与缓存体系)
9. [消息队列与异步任务](#9-消息队列与异步任务)
10. [并发、协程与常驻内存服务](#10-并发协程与常驻内存服务)
11. [分布式系统与微服务](#11-分布式系统与微服务)
12. [安全](#12-安全)
13. [性能优化与故障排查](#13-性能优化与故障排查)
14. [Linux、Nginx、容器与交付](#14-linuxnginx容器与交付)
15. [测试、质量与可观测性](#15-测试质量与可观测性)
16. [架构设计题答题框架](#16-架构设计题答题框架)
17. [项目经历与行为面试](#17-项目经历与行为面试)
18. [高频面试题自测清单](#18-高频面试题自测清单)
19. [复习路线](#19-复习路线)

---

## 1. 高级工程师的能力模型

高级工程师与中级工程师的区别，不只是“知道更多 API”，而是能对复杂系统负责：

- **深度**：能从语法讲到 Zend Engine、内存模型、进程模型和 I/O 模型。
- **广度**：理解数据库、缓存、队列、网络、操作系统、部署和监控。
- **设计能力**：能把模糊需求拆成边界清晰、可扩展、可测试的模块。
- **稳定性意识**：能预防故障，也能快速止损、定位、恢复和复盘。
- **业务意识**：技术方案与流量、成本、交付时间和业务收益相匹配。
- **领导力**：推动规范、评审、重构、知识共享，帮助团队稳定交付。

面试中的理想回答结构：

> 结论 → 原理 → 适用场景 → 不适用场景/代价 → 项目实例 → 数据结果

例如不要只说“用了 Redis 缓存”，而应说：缓存对象、数据模型、TTL、更新策略、一致性要求、穿透/击穿/雪崩措施、监控指标、故障降级，以及命中率和延迟改善。

---

## 2. PHP 语言基础与底层原理

### 2.1 类型系统与语法

必须掌握：

- 标量类型、复合类型、`callable`、`iterable`、`mixed`、`void`、`never`、`null`。
- 联合类型、交叉类型、可空类型、类型声明与 `strict_types` 的作用边界。
- 弱类型转换规则，尤其是字符串、数字、布尔值的比较陷阱。
- `==` 与 `===`、`isset()` 与 `array_key_exists()`、`empty()` 的差异。
- 值传递、引用传递、写时复制（Copy-on-Write）。
- 可变变量、可变函数、匿名函数、箭头函数和闭包变量捕获。
- 命名空间、导入、全限定名称和自动加载。
- `match` 与 `switch` 的差异：严格比较、返回值、无贯穿。
- Attribute、Enum、只读属性/类、构造器属性提升、命名参数。
- Generator 的惰性迭代、`yield` / `yield from`，适合流式处理而非“天然异步”。
- Fiber 的挂起/恢复模型；Fiber 是协作式执行原语，不等同于自动获得非阻塞 I/O。

典型追问：

1. `strict_types=1` 是否会让整个项目严格类型？  
   只影响声明所在文件发起的标量参数调用；返回值在声明函数的文件中校验，内部函数等也有边界。
2. PHP 数组为何内存占用大？  
   PHP 数组本质上是有序哈希表，同时维护键值、哈希桶和插入顺序，不是紧凑的原生数组。
3. 大文件为什么不能一次 `file_get_contents()`？  
   峰值内存与文件大小同阶，应使用流或 Generator 分块处理，并处理背压与失败恢复。

### 2.2 变量与内存

理解以下概念即可，不要求背 C 结构体字段：

- `zval` 表示 PHP 值，包含类型和值相关信息。
- 字符串、数组、对象等复杂值通过引用计数管理。
- 写时复制：赋值通常先共享，任一方发生写操作时再分离。
- 引用会改变变量绑定关系，滥用引用可能破坏写时复制并增加理解成本。
- 循环引用由垃圾回收器处理；长驻进程要关注未释放引用、静态容器和全局状态。
- `memory_limit`、峰值内存、真实分配内存的区别。

应会解释：

```php
$a = range(1, 100000);
$b = $a;       // 通常共享底层数据
$b[0] = 99;    // 写入触发分离
```

### 2.3 执行机制

请求的大致执行链：

```text
PHP 源码 → 词法/语法分析 → AST → Opcodes → Zend VM 执行
                                  ↑
                              OPcache 缓存
```

- OPcache 缓存编译后的 Opcodes，减少重复解析与编译。
- JIT 将部分热点代码编译为机器码；计算密集型可能受益，典型 Web CRUD 往往受数据库和网络 I/O 限制，收益有限。
- Preloading 可在服务启动时加载代码；需理解部署重启、内存和不可变类定义等影响。
- PHP-FPM 中每个 worker 是独立进程，单请求通常由一个 worker 同步执行。

### 2.4 生命周期与 SAPI

- 常见 SAPI：CLI、FPM/FastCGI、Apache Module。
- PHP-FPM 重点配置：`pm` 模式、`pm.max_children`、`pm.max_requests`、慢日志、请求超时。
- `static`、`dynamic`、`ondemand` 进程管理模式的吞吐、内存和冷启动取舍。
- FPM worker 数量不能拍脑袋：可用内存 ÷ 单 worker 的高分位实际内存，再结合 CPU、I/O 等待和下游容量压测。
- `max_requests` 可缓解扩展或业务代码的渐进式内存泄漏，但不是根治方案。

### 2.5 错误与异常

- `Throwable`、`Exception`、`Error` 的继承关系。
- `try/catch/finally`、多异常捕获、异常链和领域异常。
- Error Reporting 与生产环境展示/记录分离。
- 全局异常处理器和错误处理器的职责边界。
- 不要吞异常；记录 request ID、业务上下文和异常链，但不要泄露密钥及个人数据。
- 异常用于异常流程，不应用来替代普通条件分支。

### 2.6 PHP 8.x 复习重点

- PHP 8.0：联合类型、Attribute、命名参数、`match`、构造器属性提升、JIT。
- PHP 8.1：Enum、Readonly Property、Fiber、交叉类型。
- PHP 8.2+：只读类、类型系统持续增强、动态属性弃用等迁移影响。
- 迁移重点：废弃特性、类型错误更严格、扩展兼容、框架与 Composer 依赖、回归与灰度。

版本号会持续变化，面试前应查看 PHP 官方的 [Supported Versions](https://www.php.net/supported-versions.php) 与各版本迁移指南，不要把已停止支持的分支作为新项目默认选择。

---

## 3. 面向对象、设计原则与设计模式

### 3.1 OOP 基础

- 封装、继承、多态；优先组合而非继承。
- 抽象类与接口；Trait 的用途、冲突解决与滥用风险。
- `public/protected/private`、`final`、`static`、`readonly`。
- 协变返回类型与逆变参数类型。
- 魔术方法及其代价：行为隐式、静态分析困难。
- 对象比较、克隆、序列化的语义和安全风险。

### 3.2 SOLID 与代码设计

- **S**：单一职责，一个模块只有一个主要变化原因。
- **O**：对扩展开放、对修改关闭，但避免为不存在的变化过度抽象。
- **L**：子类型可替换父类型，不破坏调用方预期。
- **I**：小而聚焦的接口，避免实现方依赖无关方法。
- **D**：依赖抽象，并由外部组装具体实现。

同时理解：DRY、KISS、YAGNI、关注点分离、最少知识原则。原则之间可能冲突，需要用实际变化频率和维护成本做取舍。

### 3.3 高频设计模式

至少能说出使用场景、代码结构和缺点：

| 类别 | 模式 | 常见场景 |
|---|---|---|
| 创建型 | 工厂、抽象工厂、Builder | 按配置创建支付渠道、复杂对象组装 |
| 结构型 | Adapter、Decorator、Facade、Proxy | 三方接口适配、中间件、统一门面、远程代理 |
| 行为型 | Strategy、Observer、Template Method、Chain of Responsibility、Command、State | 促销策略、领域事件、流程骨架、中间件链、任务封装、订单状态 |

常见误区：

- 单例会制造全局状态，降低可测试性；容器中的单例生命周期也需明确。
- Repository 不应只是 ORM 的机械转发层，应表达领域查询并隔离持久化细节。
- DTO、Entity、Value Object、ORM Model 的职责不要混为一谈。

### 3.4 DDD 基础

- 限界上下文、统一语言、实体、值对象、聚合、聚合根。
- 领域服务、应用服务、Repository、Domain Event。
- 聚合边界本质上是业务一致性边界，不应为了“DDD”构造巨大对象图。
- CQRS、Event Sourcing 是可选的复杂方案，不是 DDD 的必要条件。

---

## 4. Composer 与 PHP 工程生态

- `composer.json` 与 `composer.lock`；应用应提交 lock 文件。
- `install` 按 lock 安装，`update` 重新解析并改写 lock。
- SemVer 和约束符：精确版本、`^`、`~`、范围、稳定性。
- PSR-4 自动加载映射；优化自动加载、classmap authoritative 的适用场景。
- scripts、platform config、私有仓库和插件安全。
- 生产安装常用思想：不装 dev 依赖、优化 autoload、使用确定性 lock。
- 使用 `composer audit` 和依赖更新工具治理供应链漏洞。

常用 PSR：

- PSR-3 Logger Interface
- PSR-4 Autoloading
- PSR-7 HTTP Message
- PSR-11 Container
- PSR-12 Coding Style
- PSR-14 Event Dispatcher
- PSR-15 HTTP Handlers/Middleware
- PSR-16 Simple Cache
- PSR-18 HTTP Client

面试重点不是背编号，而是解释标准怎样降低框架和组件之间的耦合。

---

## 5. Web、HTTP 与网络基础

### 5.1 HTTP

- 请求方法的安全性、幂等性和可缓存性。
- 状态码：2xx、3xx、4xx、5xx；能正确区分 400、401、403、404、409、422、429。
- Header、Cookie、Session、Cache-Control、ETag、条件请求。
- Keep-Alive、连接复用、HTTP/2 多路复用、HTTP/3 基于 QUIC 的基本认知。
- HTTPS：证书链、TLS 握手、对称/非对称加密各自的职责。
- CORS 是浏览器同源策略相关机制，不是服务端鉴权。
- RESTful API、RPC、GraphQL 的适用场景和取舍。

### 5.2 一次请求发生了什么

应能完整描述：

```text
DNS → TCP/QUIC 连接 → TLS → CDN/LB → Nginx → FastCGI/PHP-FPM
→ 路由/中间件/业务 → DB/Redis/RPC → 响应 → 日志与指标
```

并能指出每一层可能的延迟、超时、重试和故障点。

### 5.3 API 设计

- URI 表达资源，方法表达动作；统一响应和错误码但不要滥用 HTTP 200。
- 输入校验、分页、过滤、排序、字段选择、API 版本策略。
- 幂等键用于防止重复创建；服务端应原子记录请求结果。
- 鉴权与授权分离：你是谁 vs 你能做什么。
- 限流、配额、签名、防重放、审计日志。
- 超时必须层层收敛：外层超时应大于内层总预算，同时给失败处理留时间。

---

## 6. 主流框架与应用设计

Laravel、Symfony、ThinkPHP 等至少深入掌握一个，能脱离框架解释底层机制。

### 6.1 核心机制

- 请求生命周期：入口、容器启动、路由、中间件、控制器、响应、终止处理。
- IoC 容器：绑定、解析、自动装配、生命周期、循环依赖。
- Service Provider/Bundle 等扩展机制。
- Facade 的实现思想与可测试性问题。
- 路由匹配、中间件链、事件分发、队列、定时任务。
- 配置缓存、路由缓存、视图缓存，以及部署时的失效策略。

### 6.2 ORM

- Active Record 与 Data Mapper 的区别。
- 一对一、一对多、多对多、多态关联。
- N+1 查询及 Eager Loading。
- 批量读取、游标、分块处理的内存和一致性差异。
- ORM 方便但不取代 SQL 能力；复杂报表和热点查询需看执行计划。
- Mass Assignment、软删除、时间戳、模型事件的隐式副作用。

### 6.3 推荐分层

```text
Controller：协议适配、参数接收、返回响应
Application Service：用例编排、事务边界
Domain：核心业务规则
Infrastructure：DB、Redis、MQ、第三方服务
```

不必教条分层。小型 CRUD 项目可以更简单，复杂核心业务再引入领域模型，关键是依赖方向清晰、业务规则可测试。

---

## 7. MySQL 与数据库

### 7.1 数据模型与 SQL

- 三大范式与反范式；冗余换读取性能时要设计一致性维护方案。
- 常见数据类型的范围、存储和比较语义。
- 金额使用定点数或最小货币单位整数，避免浮点误差。
- `NULL` 的三值逻辑；时间类型及时区策略。
- JOIN、子查询、聚合、窗口函数、CTE。
- 深分页问题：`OFFSET` 扫描大量无用记录；可用基于游标/主键的 Seek Pagination。

### 7.2 InnoDB 与索引

- 聚簇索引：主键索引叶子节点存整行数据。
- 二级索引叶子节点保存主键值，非覆盖查询可能回表。
- B+Tree 适合范围扫描，树高低且叶子有序。
- 联合索引与最左前缀；区分筛选、排序、分组和覆盖需求。
- 索引下推、覆盖索引、回表、索引合并的概念。
- 索引失效不是固定口诀，要以优化器选择和 `EXPLAIN ANALYZE` 为准。
- 高选择性列通常更适合索引，但联合索引顺序还要综合等值、范围、排序和业务查询形态。

SQL 优化流程：

1. 用慢查询日志/APM 找到真实慢 SQL，而不是猜。
2. 获取 SQL、参数分布、表结构、索引、数据量和执行频率。
3. 查看执行计划及实际执行信息。
4. 判断扫描行数、访问类型、回表、排序、临时表、锁等待。
5. 改写 SQL/索引/模型，或引入缓存、预计算、分库分表。
6. 用相同数据分布压测，并检查写放大和其他查询是否退化。

### 7.3 事务与锁

- ACID：原子性、一致性、隔离性、持久性。
- 隔离级别及脏读、不可重复读、幻读。
- MVCC、Read View、Undo Log 的基本关系。
- 当前读与快照读。
- 记录锁、间隙锁、临键锁；隔离级别和查询条件会影响锁范围。
- 死锁不可完全避免：固定加锁顺序、缩短事务、建立合适索引，并实现有限重试。
- 长事务会占锁、延迟 purge、扩大日志和主从延迟风险。
- Redo Log 保证崩溃恢复，Undo Log 支持回滚/MVCC，Binlog 用于复制与增量恢复。

### 7.4 主从、分库分表

- 主从复制基本链路与异步复制的延迟问题。
- 读写分离会遇到“写后立即读”不一致，可按场景读主、粘滞读或等待位点。
- 分库分表先明确瓶颈；选择稳定且分布均匀的分片键。
- 跨分片 JOIN、事务、排序、分页、全局唯一 ID、扩容迁移都变复杂。
- 唯一 ID：数据库号段、UUID/ULID、Snowflake 类方案，各有有序性、可读性、时钟依赖等取舍。

---

## 8. Redis 与缓存体系

### 8.1 数据结构与场景

- String：缓存、计数、分布式锁的值。
- Hash：对象字段，注意大 Hash 与热 Key。
- List：简单队列/时间线，但可靠消息通常选专用 MQ 或 Stream。
- Set：去重、共同关系、集合运算。
- Sorted Set：排行榜、延迟任务、范围查询。
- Bitmap、HyperLogLog、Geo、Stream 的原理边界与误差/可靠性。

### 8.2 持久化与高可用

- RDB：紧凑快照、恢复快，可能丢失最近数据且 fork 有成本。
- AOF：记录写命令，数据更完整；关注刷盘策略、重写和恢复时间。
- 混合持久化、主从复制、Sentinel、Cluster。
- Cluster 按哈希槽分片；多 Key 操作需考虑同槽，可使用 hash tag。

### 8.3 缓存经典问题

- **穿透**：查不存在数据；空值缓存、布隆过滤器、参数校验。
- **击穿**：单个热点 Key 失效；互斥重建、逻辑过期、提前刷新。
- **雪崩**：大量 Key 同时过期或缓存整体不可用；TTL 抖动、多级缓存、限流降级、高可用。
- **热 Key**：单节点/单核/网卡瓶颈；拆 Key、本地缓存、读副本、业务拆分。
- **大 Key**：网络阻塞、删除抖动、迁移困难；拆分、渐进删除、扫描治理。

### 8.4 缓存一致性

常见 Cache Aside：

```text
读：先读缓存 → 未命中读 DB → 回填缓存
写：更新 DB → 删除缓存
```

这不是强一致。要继续讨论：

- 并发读写的竞态窗口和业务可接受的不一致时长。
- 删除失败的重试、消息/Outbox、CDC 订阅 Binlog。
- 延迟双删只能缩小部分窗口，也需评估延迟值、失败与并发覆盖。
- 强一致需求应考虑不使用缓存、版本号/CAS、串行化或把一致性边界重新设计。

### 8.5 分布式锁

最低要求：

- 获取锁用带过期时间的原子命令。
- value 使用唯一 token；释放时 Lua 原子比较 token 后删除。
- 临界区时长超过 TTL 时需要续期或 fencing token。
- 锁超时不等于业务一定停止，GC/网络暂停后旧持有者可能继续执行。
- 数据库唯一约束、幂等表等往往比“锁住一切”更可靠。

---

## 9. 消息队列与异步任务

需掌握 RabbitMQ、Kafka、RocketMQ 中至少一种，并能解释选择依据。

### 9.1 核心概念

- Producer、Broker、Topic/Queue、Partition、Consumer Group、Offset/Ack。
- 点对点、发布订阅、顺序消息、延迟消息、死信队列。
- 吞吐、延迟、堆积容量、消息路由和运维生态的取舍。

### 9.2 可靠性语义

- At-most-once：可能丢，不重复。
- At-least-once：不轻易丢，可能重复，最常见。
- Exactly-once 通常只在特定边界和前提下成立；业务仍应设计幂等。

端到端可靠链路：

1. 生产端确认，发送失败重试。
2. 本地事务与发消息之间用 Transactional Outbox/事务消息等解决双写。
3. Broker 持久化和副本机制。
4. 消费成功后确认，失败有限重试并进入死信队列。
5. 消费端用业务唯一键、唯一约束、状态机或幂等表去重。

### 9.3 顺序、重复与堆积

- 全局顺序吞吐低，通常只保证同一业务键进入同一分区后的局部顺序。
- 消费幂等不能只靠 Redis 短 TTL；要匹配业务数据生命周期。
- 堆积排查：生产突增、消费者报错/变慢、下游瓶颈、分区不均、消息过大。
- 扩消费者前先确认分区/队列并行度及下游承载能力。

---

## 10. 并发、协程与常驻内存服务

- 进程、线程、协程的区别；并发与并行的区别。
- 阻塞/非阻塞、同步/异步是不同维度。
- I/O 多路复用：`select/poll/epoll` 的基本思想。
- Swoole、RoadRunner 等常驻服务减少每请求启动开销，也引入状态污染和内存泄漏风险。
- 常驻模式注意：单例状态、静态变量、全局对象、连接失效、定时器、异常后清理。
- 协程环境下传统阻塞客户端可能卡住整个 worker；需要协程友好的 I/O 客户端。
- 连接池的最大连接数必须受下游容量约束；处理超时、健康检查、泄漏和归还。
- 并发修改共享状态要考虑互斥、原子操作、消息传递或无共享设计。
- 背压：下游处理不过来时，上游应限速、排队、拒绝或降级，不能无限堆内存。

---

## 11. 分布式系统与微服务

### 11.1 基础理论

- CAP 讨论的是发生网络分区时一致性与可用性的取舍，不能简化成日常“任选两个”。
- BASE 与最终一致性。
- 一致性模型：强一致、单调读、读己之写、最终一致等。
- 一致性哈希、虚拟节点及扩缩容数据迁移。
- 服务发现、配置中心、API Gateway、负载均衡。

### 11.2 稳定性模式

- 超时：所有远程调用必须有明确超时。
- 重试：只对可恢复错误，并结合幂等、指数退避、抖动和总次数上限。
- 熔断：错误率达到阈值时快速失败，经过半开状态探测恢复。
- 限流：固定窗口、滑动窗口、漏桶、令牌桶。
- 隔离：线程/进程/连接池/队列/租户隔离，防止故障扩散。
- 降级：关闭非核心功能、返回兜底或旧数据。
- 舱壁与负载丢弃：容量耗尽前主动保护核心链路。

警惕“重试风暴”：调用链每层都重试会指数放大流量。通常由靠近用户或最了解语义的一层统一控制重试预算。

### 11.3 分布式事务

- 2PC：强一致倾向，但阻塞、协调者和可用性成本高。
- TCC：Try/Confirm/Cancel，对业务侵入大，需防悬挂、空回滚、重复调用。
- Saga：长事务拆成本地事务与补偿，适合可补偿业务。
- Outbox + MQ：数据库事务内写业务数据和事件表，异步投递，最终一致。
- 选择前先问：业务是否真的需要跨服务强一致？能否调整边界、状态机或人工补偿？

### 11.4 微服务取舍

适合：团队和业务边界成熟、独立扩缩容/发布需求明确。  
代价：网络不可靠、数据一致性、调用链排查、测试、部署和组织协作成本显著增加。单体并非落后，模块化单体常是更稳妥的起点。

---

## 12. 安全

### 12.1 常见攻击与防护

- SQL 注入：参数化查询；不要拼接用户输入，动态表名/排序字段使用白名单。
- XSS：按输出上下文编码；HTML、属性、JS、URL 上下文不同；配合 CSP。
- CSRF：CSRF Token、SameSite Cookie、校验 Origin/Referer；不能用 CORS 代替。
- SSRF：协议和域名/IP 白名单，解析后校验，阻止内网与云元数据地址，限制跳转和响应体。
- 文件上传：校验内容类型和文件头、随机文件名、大小限制、站外/对象存储、禁止执行。
- 命令注入：避免 Shell；必须使用时严格参数化/白名单。
- 路径穿越、反序列化漏洞、XXE、开放重定向、Host Header 攻击。

### 12.2 身份认证与权限

- Session 与 JWT 的优缺点；JWT 难以即时撤销，不能存敏感明文数据。
- Access Token 短期有效，Refresh Token 轮换并检测重放。
- RBAC、ABAC、数据权限与最小权限原则。
- 密码使用专用慢哈希算法，通过 `password_hash()` / `password_verify()`；不可使用 MD5/SHA 直接存密码。
- MFA、登录限速、撞库防护、敏感操作二次确认。

### 12.3 密钥与数据安全

- 密钥不入代码库和镜像；使用 Secret 管理，支持轮换和审计。
- 传输加密、存储加密、字段级加密；区分加密、哈希和编码。
- 日志脱敏；禁止记录密码、完整 Token、银行卡等敏感数据。
- 依赖漏洞扫描、最小化容器镜像、及时升级已停止支持的 PHP 分支。

---

## 13. 性能优化与故障排查

### 13.1 方法论

> 先测量，再定位，再优化，再验证。不要凭感觉改代码。

关注指标：

- 流量：QPS/TPS、并发、请求/响应大小。
- 延迟：平均值不够，要看 P50/P95/P99/P999。
- 错误：业务错误、HTTP 5xx、超时、重试、熔断。
- 饱和度：CPU、内存、Load、磁盘 I/O、网络、连接池、线程/进程/队列。

### 13.2 排查顺序

1. 明确影响范围、开始时间、变更记录，先止损。
2. 看入口流量、错误率、延迟分位数和资源饱和度。
3. 沿 Trace 拆分 Nginx、PHP、SQL、Redis、RPC、MQ 耗时。
4. 用日志和 Profiling 找具体错误与热点函数。
5. 检查数据库慢查询/锁、缓存命中率/热 Key、连接池和队列堆积。
6. 形成假设，用证据验证；修复后观察并复盘。

### 13.3 常用优化手段

- PHP：升级受支持版本、OPcache、减少无效序列化和大数组、批处理、惰性迭代。
- 应用：消除 N+1、减少远程往返、并行独立 I/O（运行模型允许时）、合理缓存。
- DB：索引/SQL/模型优化、批量写、读写分离、归档，最后才考虑分片。
- Redis：Pipeline/Lua 减少 RTT，治理热 Key/大 Key，避免危险全量命令。
- 网络：压缩、连接复用、CDN、控制 payload。
- 架构：异步化、预计算、限流降级，但必须接受相应一致性和复杂度成本。

### 13.4 容量估算

面试中展示推导过程：

```text
平均 QPS = 日请求量 / 86400
峰值 QPS = 平均 QPS × 峰值系数
并发量 ≈ QPS × 平均响应时间（秒）
存储量 ≈ 每条大小 × 每日条数 × 保留天数 × 副本/索引系数
```

给容量留冗余，并用压测校准。Little's Law 的前提是相对稳定的系统，故障拥塞时不能机械套用。

---

## 14. Linux、Nginx、容器与交付

### 14.1 Linux

- 进程/线程、文件描述符、信号、虚拟内存、Page Cache、Swap。
- CPU user/system/iowait、Load Average 与 CPU 使用率的区别。
- 常用排障：`top`/`pidstat`/`vmstat`/`iostat`/`ss`/`lsof`/`strace`/`dmesg`。
- OOM、磁盘满、inode 满、连接数耗尽、端口耗尽的现象与定位。
- 定时任务、日志轮转、systemd/Supervisor 管理进程。

### 14.2 Nginx 与 PHP-FPM

- Nginx event loop 和反向代理基本原理。
- `location` 匹配、`try_files`、FastCGI 参数、安全传递脚本路径。
- 负载均衡、健康检查、限流、缓存、静态资源。
- 区分连接超时、读写超时和上游超时。
- 502/504 排查：上游存活、socket/端口、FPM 饱和、慢请求、超时配置、日志。

### 14.3 Docker/Kubernetes/CI-CD

- 镜像分层、多阶段构建、固定依赖版本、非 root 用户、健康检查。
- 容器是隔离进程，不是轻量虚拟机；应用日志输出到标准流。
- K8s：Pod、Deployment、Service、Ingress、ConfigMap、Secret、Probe、HPA 基本概念。
- Readiness 控制接流量，Liveness 用于检测是否需要重启，Startup Probe 保护慢启动。
- CI：静态检查、测试、安全扫描、构建不可变制品。
- CD：滚动、蓝绿、金丝雀；数据库变更采用向前/向后兼容的 Expand-Contract。
- 回滚不只回代码，还要考虑数据库 Schema、消息格式和缓存结构兼容。

---

## 15. 测试、质量与可观测性

### 15.1 测试

- 单元测试：快、隔离，验证业务规则。
- 集成测试：验证 DB、Redis、MQ、HTTP 等真实协作。
- 契约测试：约束服务消费者与提供者的接口兼容。
- 端到端测试：覆盖关键业务链路，不追求覆盖所有组合。
- Mock 适合隔离边界，不要 Mock 被测对象内部实现细节。
- 测试覆盖率是信号，不等于测试质量；重点覆盖核心规则、边界和失败路径。
- PHPUnit/Pest、静态分析（PHPStan/Psalm）、代码风格、复杂度和重复度检查。

### 15.2 可观测性

三大支柱：

- Metrics：适合聚合、趋势和告警。
- Logs：适合离散事件和上下文调查。
- Traces：适合跨服务调用链和耗时拆解。

关键实践：

- 全链路统一 trace ID/request ID。
- 结构化日志，字段稳定，级别合理，敏感字段脱敏。
- RED：Rate、Errors、Duration；资源层用 USE：Utilization、Saturation、Errors。
- 告警应可行动，围绕用户影响和 SLO，避免静态阈值噪声。
- SLI 是测量指标，SLO 是目标，Error Budget 用于平衡发布速度和可靠性。

### 15.3 Code Review

审查优先级：正确性与安全 > 数据一致性 > 可维护性 > 性能 > 风格。关注异常路径、并发竞态、事务边界、幂等、资源释放、兼容性和测试，而不是只挑命名。

---

## 16. 架构设计题答题框架

以“设计秒杀/订单/短链/排行榜/日志平台”为例，按以下顺序回答。

### 16.1 七步法

1. **澄清需求**：核心功能、用户规模、峰值 QPS、延迟、数据量、一致性、可用性。
2. **做容量估算**：流量、并发、带宽、存储、增长周期。
3. **定义接口和数据模型**：关键 API、表、索引、状态机、幂等键。
4. **画高层链路**：入口、服务、缓存、数据库、队列、对象存储。
5. **深入关键难点**：并发扣减、热点、一致性、顺序、容灾。
6. **考虑失败场景**：超时、重试、重复、部分失败、降级、数据修复。
7. **补充交付与演进**：监控、压测、安全、灰度、成本和分阶段扩展。

### 16.2 秒杀题应覆盖

- CDN/静态化、鉴权和资格预热、网关限流。
- 库存预热与原子扣减，防超卖也防少卖。
- 请求入队削峰，创建订单与支付超时状态机。
- 一人一单用唯一约束/幂等键兜底。
- MQ 重复、丢失、堆积处理；数据库最终对账和补偿。
- 热点隔离、降级开关、容量压测和应急预案。

### 16.3 方案权衡模板

```text
当前规模下我先选择 A，因为它满足 X/Y，复杂度和成本较低。
它的限制是 Z；当指标达到阈值 T 时，再演进到 B。
为了降低迁移成本，现在会预留接口/数据边界 C，但不会提前引入整套 B。
```

---

## 17. 项目经历与行为面试

### 17.1 准备 3 个故事

至少准备：

1. 最有技术深度的项目。
2. 一次线上故障与复盘。
3. 一次跨团队推动或技术债治理。

用 STAR 组织，但技术面要增加“决策与验证”：

- Situation：业务背景、规模、痛点。
- Task：你的职责和目标。
- Action：候选方案、为何选择、你亲自做了什么、风险控制。
- Result：量化结果。
- Reflection：不足、复盘、如果重做会怎样。

### 17.2 可量化指标

- P99 从多少降到多少。
- QPS/吞吐提升多少，机器/云成本下降多少。
- 错误率、告警数、故障恢复时间降低多少。
- 发布耗时、回滚耗时、测试覆盖或缺陷逃逸率变化。
- 不知道精确值时明确说是监控区间或估算，不要编造。

### 17.3 高级岗位常见软技能题

- 你如何推动一项有争议的重构？
- 产品要求不合理工期时怎么处理？
- 如何评估和偿还技术债？
- 如何带新人、做 Code Review、建立规范？
- 技术方案被否决怎么办？
- 如何处理跨团队依赖和线上事故责任？

回答原则：用事实和数据沟通，给出备选方案与风险，不甩锅，展示闭环能力。

---

## 18. 高频面试题自测清单

如果以下问题不能在 2～5 分钟内清晰回答，应继续复习。

### PHP

- [ ] PHP 数组底层为何占内存？什么时候换用其他数据结构？
- [ ] 引用计数、写时复制、垃圾回收分别解决什么问题？
- [ ] OPcache 与 JIT 有什么区别？Web 项目为何不一定受益于 JIT？
- [ ] PHP-FPM `max_children` 如何估算？设置过大有什么后果？
- [ ] Generator、Fiber、协程、异步 I/O 的关系是什么？
- [ ] 常驻内存模式与传统 FPM 模式的开发差异是什么？
- [ ] 容器如何自动装配？依赖注入带来了什么代价？

### MySQL

- [ ] 为什么 InnoDB 主键推荐短小、稳定、趋势递增？递增也有哪些热点代价？
- [ ] 联合索引 `(a,b,c)` 分别能支持哪些查询和排序？
- [ ] 为什么会回表？怎样构造覆盖索引？
- [ ] MVCC 如何实现快照读？不同隔离级别的 Read View 有何差异？
- [ ] 当前读为什么可能产生间隙锁？
- [ ] 死锁如何定位、降低概率和安全重试？
- [ ] 慢 SQL 的完整排查流程是什么？
- [ ] 深分页和主从延迟如何治理？

### Redis

- [ ] Redis 快的原因是什么？“单线程”表述有哪些边界？
- [ ] RDB、AOF 的取舍和可能的数据损失窗口是什么？
- [ ] 缓存穿透、击穿、雪崩、热 Key、大 Key分别如何治理？
- [ ] Cache Aside 为何仍会不一致？业务上怎样选择方案？
- [ ] 分布式锁怎样避免误删？过期后旧客户端继续执行怎么办？
- [ ] Cluster 扩容与多 Key 操作要注意什么？

### MQ 与分布式

- [ ] 怎样保证消息尽量不丢？怎样处理重复消费？
- [ ] 数据库提交成功但消息发送失败怎么办？
- [ ] 如何保证同一订单消息有序？
- [ ] 消息堆积如何定位和扩容？
- [ ] CAP 的网络分区前提是什么？
- [ ] 重试为什么会放大故障？如何设置重试预算？
- [ ] TCC、Saga、Outbox 分别适合什么场景？
- [ ] 微服务拆分的依据和代价是什么？

### Web、安全与运维

- [ ] 浏览器发起 HTTPS 请求后发生了什么？
- [ ] Cookie、Session、JWT 如何选择？
- [ ] CORS 与 CSRF 是一回事吗？
- [ ] SQL 注入、XSS、SSRF、文件上传漏洞分别怎样防？
- [ ] 502 与 504 常见原因和排查路径是什么？
- [ ] Load 很高但 CPU 不高，可能是什么问题？
- [ ] 发布时数据库字段变更怎样兼容新旧代码？
- [ ] 如何定义 SLI/SLO 和有效告警？

### 架构与项目

- [ ] 设计一个高并发秒杀系统。
- [ ] 设计订单超时关闭系统。
- [ ] 设计短链接系统并估算容量。
- [ ] 讲一次最严重线上故障：发现、止损、定位、恢复、复盘。
- [ ] 讲一次性能优化，必须给出优化前后指标和验证方法。
- [ ] 讲一次技术选型，说明被放弃的方案和原因。

---

## 19. 复习路线

### 第一阶段：夯实基础（1～2 周）

- PHP 类型/OOP/异常/内存/执行流程。
- HTTP、TCP/TLS、Linux 基础。
- 手写小实验验证 COW、Generator、事务隔离和锁。

### 第二阶段：核心中间件（2～3 周）

- MySQL 索引、执行计划、事务、锁、复制。
- Redis 数据结构、缓存一致性、持久化、高可用。
- MQ 可靠性、顺序、幂等、堆积。

### 第三阶段：架构与工程（1～2 周）

- 框架生命周期、容器、ORM 和分层。
- 分布式一致性、稳定性模式、安全、可观测性。
- Docker/K8s、CI/CD、灰度与回滚。

### 第四阶段：面试演练（持续）

- 每天口述 5 个高频题，录音后检查是否有结论、原理、场景和取舍。
- 每周完成 2 道系统设计题，先估算再画架构。
- 把自己的 3 个项目故事压缩成 3 分钟版和扩展成 15 分钟版。
- 对简历每个技术名词准备至少两层追问，删除只听过但讲不清的名词。

### 面试前最后检查

- [ ] 能在 1 分钟内做与岗位匹配的自我介绍。
- [ ] 熟悉简历中每一项指标的来源和计算方式。
- [ ] 能画出当前项目核心链路、数据流和部署架构。
- [ ] 能说出项目最大瓶颈、当前妥协和下一步演进条件。
- [ ] 准备了对公司的问题：业务挑战、团队协作、质量体系、岗位预期。

---

## 结语

这份清单的目标不是让你背下所有名词，而是形成一张能关联起来的知识网络。高级工程师最有说服力的表达通常是：

> 我知道这个方案为什么有效，也知道它什么时候会失效；我能用数据找到瓶颈，用约束做取舍，并为失败设计恢复路径。

