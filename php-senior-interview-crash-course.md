# PHP 高级工程师面试突击手册

> 目标：面试前快速复习，解决“知道，但临场讲不清”的问题。  
> 使用方法：先遮住答案口述 1～3 分钟，再看“回答要点”和“继续追问”。

## 回答问题的万能结构

技术题尽量按下面的顺序回答：

1. **先说结论**：一句话回答问题。
2. **再讲原理**：它为什么是这样。
3. **说明场景**：什么时候应该用。
4. **指出代价**：有什么风险和限制。
5. **结合项目**：你怎么做、结果如何。

推荐话术：

> 我的理解是……，它的核心原理是……。在我们的场景中，因为……所以选择了……。这个方案的代价是……，我们通过……控制风险。最终 P99/错误率/成本从……变成了……。

---

## 一、PHP 高频问题

### 1. PHP 数组底层是什么？为什么占用内存大？

**回答要点：**

PHP 数组本质上是一个有序哈希表，不只是连续内存中的普通数组。它既支持整数键，也支持字符串键，还要维护哈希桶、键值、插入顺序和 `zval`，所以功能灵活，但内存开销较大。

如果是百万级纯数字集合，可考虑：

- Generator 流式处理，避免全部加载。
- `SplFixedArray` 或扩展提供的紧凑结构。
- 把聚合、过滤下推到数据库。
- 分块读取和批量处理。

**继续追问：**为什么 PHP 数组查询通常是 O(1)？哈希冲突怎么处理？什么时候发生扩容？

### 2. 什么是写时复制（Copy-on-Write）？

**回答要点：**

变量赋值时，PHP 通常不会立刻复制数组或字符串的数据，而是让两个变量共享底层值并增加引用计数。当其中一个变量发生写操作时，才复制一份数据。这可以减少不必要的内存复制。

```php
$a = range(1, 100000);
$b = $a;       // 此时通常共享数据
$b[0] = 100;   // 写入时发生分离
```

传引用和某些修改操作会影响该机制。面试中不要把“变量赋值”简单说成必然深拷贝。

### 3. `==` 和 `===` 有什么区别？

**回答要点：**

- `==` 会进行类型转换后比较。
- `===` 同时比较类型和值。

业务代码应优先使用严格比较，避免弱类型转换产生意外结果。用户输入、数据库结果和 JSON 数据尤其需要明确类型。

### 4. `isset()`、`empty()`、`array_key_exists()` 有什么区别？

**回答要点：**

- `isset($arr['x'])`：键存在且值不为 `null` 才是 `true`。
- `array_key_exists('x', $arr)`：只检查键是否存在，值可以为 `null`。
- `empty()`：不存在或值被视为假值时为 `true`，例如 `0`、`'0'`、`false`、`[]`、`null`。

### 5. PHP 的垃圾回收机制是什么？

**回答要点：**

PHP 对复杂值主要使用引用计数。当引用计数降到 0 时可以释放；但循环引用不会自然降到 0，因此垃圾回收器还会识别和回收循环引用。

传统 FPM 请求结束后进程会清理请求级资源；Swoole、RoadRunner 等常驻进程中，对象可能跨请求残留，更需要关注静态属性、单例、闭包引用、事件监听器和全局容器导致的内存增长。

### 6. OPcache 和 JIT 有什么区别？

**回答要点：**

- OPcache 缓存 PHP 编译后的 Opcodes，避免每次请求重复解析和编译，Web 项目通常应开启。
- JIT 把部分热点 Opcodes 编译为机器码，主要改善 CPU 密集计算。
- 典型 CRUD 系统的瓶颈常在数据库、缓存和网络 I/O，因此 JIT 不一定有明显收益。

### 7. PHP-FPM 的运行模式有哪些？

**回答要点：**

- `static`：固定 worker 数，性能稳定，但空闲时也占内存。
- `dynamic`：按配置动态调整 worker，通用场景最常见。
- `ondemand`：有请求时创建，节省空闲内存，但有冷启动成本。

`pm.max_children` 不能只按 CPU 核数设置，应结合：

```text
可供 FPM 使用的内存 ÷ 单 worker 高分位内存
```

同时考虑 CPU、I/O 等待、数据库连接上限和压测结果。设置过大会造成内存耗尽、上下文切换和下游连接风暴。

### 8. PHP-FPM 请求完整流程是什么？

**口述版：**

> 请求经 DNS、负载均衡到达 Nginx。Nginx 通过 FastCGI 把动态请求交给 PHP-FPM，FPM 的 worker 初始化 PHP 运行环境，框架完成容器启动、路由、中间件和业务处理，然后访问 MySQL、Redis 或其他服务，最终将响应交回 Nginx。OPcache 可避免 PHP 文件被重复编译。

### 9. Generator 有什么用？

**回答要点：**

Generator 通过 `yield` 惰性地产生值，适合遍历大文件、大结果集和数据管道，避免一次把全部数据放入内存。它解决的是惰性迭代问题，不等于异步，也不会自动减少数据库执行时间。

### 10. Fiber、协程、异步是什么关系？

**回答要点：**

Fiber 是可以挂起和恢复执行栈的语言级原语。协程框架可以基于 Fiber 或扩展实现调度，但只有配合事件循环和非阻塞 I/O，才能在等待网络时切换执行其他任务。Fiber 本身不会把阻塞 MySQL 客户端自动变成异步客户端。

### 11. Trait、抽象类、接口如何选择？

- 接口定义能力契约，适合解耦和多实现。
- 抽象类可共享状态和部分实现，但只有单继承。
- Trait 是代码复用机制，适合横向复用少量实现。

优先依赖接口；Trait 不应成为堆放杂乱公共方法的工具。

### 12. `include`、`require`、`include_once`、`require_once` 的区别？

- `require` 失败会产生致命错误，`include` 失败通常是警告。
- `_once` 会避免重复加载，但有额外检查。
- 现代项目通常依赖 Composer 的 PSR-4 自动加载，不手工组织大量 `require_once`。

### 13. Composer 的 `install` 和 `update` 有什么区别？

- `install` 按 `composer.lock` 安装确定版本。
- `update` 根据 `composer.json` 重新解析依赖，并更新 lock。

应用项目应提交 `composer.lock`；生产环境使用 `install`，保证不同环境依赖一致。

### 14. 依赖注入和 IoC 容器解决了什么问题？

依赖注入把对象依赖从类内部创建改成外部提供，使业务类依赖抽象，便于替换实现和测试。IoC 容器负责对象绑定、解析和生命周期管理。

代价是调用关系可能变得隐式，自动装配错误更晚暴露，容器也可能被滥用成全局 Service Locator。

---

## 二、框架与代码设计

### 15. Laravel/Symfony 的请求生命周期怎么讲？

**通用口述版：**

> 请求从入口文件进入，加载 Composer 自动加载器，启动应用和服务容器，注册并启动服务提供者，匹配路由，依次执行全局及路由中间件，调用控制器或处理器，业务层完成用例，响应再反向经过中间件返回。终止阶段还可能执行日志、会话写入或 terminate hook。

应结合自己使用的框架讲出具体扩展点，不要只背通用链路。

### 16. 什么是 N+1 查询？

先查询一批父记录，再在循环中逐条查询关联记录，最终形成 1+N 次查询。

解决方式：

- Eager Loading。
- JOIN 或批量 `WHERE IN`。
- DataLoader 式批量聚合。
- 对复杂场景按访问模式重新设计查询。

优化后要检查是否产生巨大 JOIN、重复行或一次加载过多对象。

### 17. Active Record 和 Data Mapper 的区别？

- Active Record：对象同时包含数据和持久化行为，上手快，适合 CRUD。
- Data Mapper：领域对象与持久化分离，适合复杂领域，但抽象层和代码量更多。

不要为了模式而模式，依据业务复杂度和团队能力选择。

### 18. Repository 层有什么价值？

Repository 用领域语言表达数据访问，隔离持久化细节，让应用/领域层不直接依赖 ORM 查询语法。若只是把 ORM 的每个方法机械包装一遍，通常价值不大，反而增加样板代码。

### 19. 如何理解 SOLID？

- 单一职责：一个模块只有一个主要变化原因。
- 开闭原则：通过扩展支持变化，避免反复修改稳定代码。
- 里氏替换：子类不能破坏父类的行为契约。
- 接口隔离：调用方不应依赖不需要的方法。
- 依赖倒置：高层策略依赖抽象，而不是具体基础设施。

面试时最好给一个支付渠道、通知渠道或存储驱动的实际例子。

### 20. 常用设计模式怎么回答？

优先讲自己真正用过的：

- Strategy：按场景选择支付、定价、路由策略。
- Factory：根据配置创建渠道实现。
- Adapter：统一第三方接口差异。
- Decorator：在客户端外增加缓存、重试、日志。
- Chain of Responsibility：中间件、风控规则链。
- Observer/Event：业务完成后解耦非核心动作。
- State：订单、工单状态迁移。

每个模式都要能说明“不使用它会怎样”和“引入后的复杂度”。

---

## 三、MySQL 高频问题

### 21. 为什么 MySQL 索引常用 B+Tree？

B+Tree 分支多、树高低，可以用较少磁盘 I/O 定位数据；数据集中在叶子节点，叶子节点有序连接，既适合等值查询，也适合范围和排序查询。

相较之下：

- 哈希索引适合等值查询，不适合范围和有序扫描。
- 普通二叉树分支少，数据量大时树高更高。

### 22. 什么是聚簇索引和回表？

InnoDB 主键索引的叶子节点存整行数据，因此是聚簇索引。二级索引叶子节点通常存索引列和主键值。通过二级索引找到主键后，再访问主键索引获取其他列，称为回表。

如果查询所需字段都在某个索引中，就形成覆盖索引，可避免回表。

### 23. 联合索引为什么有最左前缀原则？

联合索引按定义的列顺序逐层排序。例如 `(a,b,c)` 首先按 `a` 排，相同 `a` 下再按 `b` 排。因此跳过 `a` 时，`b` 在全局上并不有序，通常无法直接完成高效定位。

要避免背“索引必然失效”的口诀。优化器可能使用索引扫描、索引下推或其他策略，最终看执行计划和实际扫描量。

### 24. 哪些情况可能导致索引效果差？

- 对索引列做函数或不匹配的隐式类型转换。
- 联合索引不符合查询的访问顺序。
- 前导模糊匹配，如 `LIKE '%abc'`。
- 低选择性条件返回大部分数据。
- 范围、排序、分组组合无法被当前索引同时满足。
- 统计信息不准，优化器判断错误。

正确回答应补一句：用真实参数和 `EXPLAIN ANALYZE` 验证，不能只靠规则猜。

### 25. `EXPLAIN` 主要看什么？

- 访问方式和实际扫描行数。
- 使用了哪个索引。
- 索引使用了哪些列。
- 预估行数与实际行数是否偏差很大。
- 是否有额外排序、临时表、大量回表。
- 各执行节点实际耗时。

SQL 优化的目标不是“必须用索引”，而是减少不必要的数据访问和总耗时。

### 26. 事务 ACID 是什么？

- Atomicity：事务内操作全部成功或全部失败。
- Consistency：事务使数据从一个合法状态进入另一个合法状态。
- Isolation：并发事务相互影响受到隔离级别约束。
- Durability：提交后的结果在故障后仍能恢复。

Redo Log 主要支持持久性和崩溃恢复，Undo Log 支持回滚与 MVCC，锁和 MVCC共同支持隔离性。

### 27. 四种隔离级别有什么区别？

| 隔离级别 | 脏读 | 不可重复读 | 幻读 |
|---|---:|---:|---:|
| Read Uncommitted | 可能 | 可能 | 可能 |
| Read Committed | 避免 | 可能 | 可能 |
| Repeatable Read | 避免 | 避免 | 需结合 MVCC/锁理解 |
| Serializable | 避免 | 避免 | 避免 |

InnoDB 的 RR 中，普通快照读通过 MVCC 获得一致性视图；当前读通过锁机制处理并发。不要简单回答“RR 绝对不存在任何幻读”，要区分快照读、当前读和具体操作序列。

### 28. MVCC 是怎么实现的？

核心是：记录中的事务信息、Undo Log 版本链和 Read View。查询根据当前事务的可见性规则，从版本链中找到可见版本，从而让许多读操作不必阻塞写操作。

- RC 通常每条语句建立新的 Read View。
- RR 通常在事务首次一致性读时建立 Read View，并在事务内复用。

### 29. 什么是当前读和快照读？

- 普通一致性 `SELECT` 通常是快照读，读取符合 Read View 的版本。
- `SELECT ... FOR UPDATE`、`UPDATE`、`DELETE` 等是当前读，读取最新可用版本并按需加锁。

### 30. InnoDB 有哪些锁？

- 记录锁：锁定索引记录。
- 间隙锁：锁定索引记录之间的区间。
- 临键锁：记录锁与间隙锁的组合。
- 意向锁：用于表级与行级锁协调。

锁通常加在索引上。索引不合适可能扫描并锁住比预期更多的记录或区间。

### 31. 数据库死锁怎么办？

死锁发生后，数据库会选择一个事务回滚。应用要识别死锁错误并做有限、带退避的重试。

降低概率：

- 多个事务按相同顺序访问资源。
- 缩短事务，事务内不做远程调用。
- 建立合适索引，减少扫描和锁范围。
- 大事务拆小，但要重新评估原子性。

排查要看死锁日志、具体 SQL、索引和加锁顺序。

### 32. 慢 SQL 如何排查？

**标准回答：**

1. 从 APM/慢查询日志定位 SQL、频率和真实参数。
2. 查看表结构、索引、数据量和数据分布。
3. 用 `EXPLAIN ANALYZE` 查看预估与实际执行。
4. 判断扫描行数、回表、排序、临时表和锁等待。
5. 选择改索引、改 SQL、改数据模型或缓存/预计算。
6. 在相似数据量下压测，并确认没有拖慢其他写入和查询。

### 33. 深分页为什么慢？

`LIMIT 100000, 20` 往往仍需扫描或读取前面的 100000 行再丢弃。可以采用游标/Seek 分页：记录上页最后一个稳定排序键，用 `WHERE id > ? ORDER BY id LIMIT 20` 获取下一页。

如果按非唯一列排序，应使用复合游标，例如 `(created_at, id)`，保证顺序稳定。

### 34. 主从延迟如何处理？

- 写后立即读的关键请求读主库。
- 用户会话内短时间粘滞主库。
- 根据复制位点等待从库追平。
- 页面允许时接受短暂旧数据。

还应治理根因：大事务、慢 SQL、从库性能不足、单线程应用瓶颈或网络问题。

### 35. 分库分表要考虑什么？

- 先确认单库瓶颈是否真实存在。
- 选择稳定、分布均匀且符合主要查询的分片键。
- 处理跨分片 JOIN、聚合、排序、分页和事务。
- 设计全局唯一 ID。
- 设计扩容迁移、双写校验、灰度切换和回滚。

分库分表会把数据库问题升级为分布式系统问题，不应过早引入。

---

## 四、Redis 高频问题

### 36. Redis 为什么快？

- 数据主要在内存中。
- 核心命令执行路径高效，数据结构针对场景优化。
- 主要命令执行串行化，避免大量共享数据锁竞争。
- 使用 I/O 多路复用处理大量连接。

“Redis 是单线程”是不完整的说法：不同版本和功能中还可能有后台线程、I/O 线程、持久化子进程等；通常说的是核心命令执行模型。

### 37. Redis 常用数据结构及场景？

- String：缓存、计数、Token。
- Hash：对象字段。
- List：简单队列、时间线。
- Set：去重、共同关注。
- Sorted Set：排行榜、按分值范围查询。
- Bitmap：签到、布尔状态统计。
- HyperLogLog：允许误差的基数统计。
- Stream：消费组和流式消息。

### 38. RDB 和 AOF 有什么区别？

- RDB 是时间点快照：文件紧凑、恢复快，但可能丢失上次快照后的数据，fork 和持久化也有资源成本。
- AOF 记录写操作：可配置更小的数据丢失窗口，但文件和重写成本更高。

生产选择要结合数据可重建性、允许丢失窗口、恢复时间和磁盘性能，而不是只说“两个都开最好”。

### 39. 缓存穿透、击穿、雪崩分别是什么？

- 穿透：反复查询不存在的数据。方案：参数校验、短期空值缓存、布隆过滤器。
- 击穿：单个热点 Key 失效，大量请求同时访问数据库。方案：互斥重建、逻辑过期、提前刷新。
- 雪崩：大量 Key 同时过期或 Redis 整体故障。方案：TTL 加随机值、多级缓存、高可用、限流和降级。

### 40. 什么是热 Key 和大 Key？

- 热 Key：访问集中在少数 Key，造成单节点、单核或网卡瓶颈。可以拆分、本地缓存、读副本、热点隔离。
- 大 Key：单个 Key 包含大量元素或值很大，造成网络阻塞、删除抖动和迁移困难。要拆分、限制大小、渐进扫描和删除。

排查应结合 Redis latency、slowlog、命令统计、采样扫描和业务访问日志，避免在线执行阻塞式全量命令。

### 41. 如何保证缓存和数据库一致？

常用 Cache Aside：

```text
读：读缓存 → 未命中读数据库 → 回填缓存
写：更新数据库 → 删除缓存
```

先更新数据库再删除缓存通常更实用，但仍然不是强一致。删除失败需要重试，可通过 Outbox、MQ 或订阅 Binlog 最终修正。

回答时先明确业务能容忍多久不一致：

- 能容忍短暂不一致：Cache Aside + TTL + 删除重试。
- 要更及时：CDC/Binlog 驱动失效。
- 不能容忍：关键读走数据库、版本校验、串行化，或干脆不缓存该数据。

### 42. Redis 分布式锁怎样实现？

基本实现：

1. 原子地设置 `NX` 和过期时间。
2. value 使用当前持有者唯一 token。
3. 释放时用 Lua 脚本比较 token 后删除。
4. 业务可能超过 TTL 时考虑续期。

但锁过期后，旧持有者仍可能继续执行。严格场景还需要 fencing token，或直接依靠数据库唯一约束、状态条件更新等最终保护。

### 43. Redis 的过期键是怎么删除的？

通常结合：

- 惰性删除：访问 Key 时发现过期再删。
- 定期删除：周期性抽样检查并删除过期 Key。

内存达到上限时还会依据配置的淘汰策略处理 Key。过期策略和淘汰策略不是一回事。

### 44. 布隆过滤器有什么特点？

布隆过滤器节省空间，可判断“某元素一定不存在”或“可能存在”。它有假阳性，但传统实现没有假阴性；删除通常困难，需要计数布隆等变体。

---

## 五、消息队列与分布式系统

### 45. 为什么使用消息队列？

- 异步：把非核心操作移出主链路。
- 解耦：生产者不依赖消费者具体实现。
- 削峰：把瞬时流量转成可控消费速率。

代价是引入最终一致性、重复消费、消息堆积、顺序和运维复杂度。

### 46. 如何保证消息不丢？

要按整个链路回答：

1. 生产端启用确认，失败重试。
2. 数据库与消息双写使用事务消息或 Transactional Outbox。
3. Broker 启用持久化和副本机制。
4. 消费成功后再 Ack。
5. 失败有限重试，超过次数进入死信队列并告警。
6. 定期对账和补偿。

不存在脱离具体中间件配置和故障模型的“绝对不丢”。

### 47. 如何解决重复消费？

使用业务唯一键设计幂等：

- 数据库唯一约束。
- 幂等记录表。
- 状态机条件更新，例如只允许 `pending → paid`。
- 外部接口传递幂等键。

只用 Redis `SETNX` 且 TTL 很短可能在 Key 过期后再次执行，不能作为所有场景的最终兜底。

### 48. 数据库提交成功，但 MQ 发送失败怎么办？

典型方案是 Transactional Outbox：在同一个数据库事务中写业务表和事件表，独立投递程序把事件发送到 MQ，发送成功后标记。投递可能重复，所以消费者仍需幂等。

也可使用消息中间件提供的事务消息，但需要理解其回查和状态恢复机制。

### 49. 如何保证消息顺序？

通常只保证同一业务实体的局部顺序：把相同订单 ID 路由到同一分区/队列，并由单个逻辑消费者串行处理。

还要处理重试造成的乱序。可使用业务版本号、状态机校验、延迟重试旁路或按实体暂存重排。

### 50. 消息积压怎么处理？

1. 先确认生产速度、消费速度、Lag 和开始时间。
2. 检查消费者错误、慢处理、下游 DB/接口瓶颈和分区倾斜。
3. 修复根因后再增加消费者；并行度不能超过分区/队列能力。
4. 下游无法承受时限速，不能盲目扩容把数据库打垮。
5. 必要时临时扩分区、批量消费或转储后离线处理。

### 51. CAP 怎么解释？

CAP 描述分布式系统发生网络分区时，只能在一致性和可用性之间做取舍。网络分区不是正常情况，但系统必须决定分区期间是拒绝部分请求保证一致，还是继续服务并接受数据不一致。

CAP 不能简单理解成任何时候都从 C、A、P 中任意选两个。

### 52. 什么是限流、熔断、降级？

- 限流：控制进入系统或资源的流量。
- 熔断：下游持续失败时暂时停止调用，避免资源耗尽和故障扩散。
- 降级：关闭非核心能力或返回兜底结果，保护核心链路。

三者目的相关，但发生位置和触发条件不同。

### 53. 常见限流算法？

- 固定窗口：简单，但窗口边界可能突发双倍流量。
- 滑动窗口：更平滑，状态和计算更复杂。
- 漏桶：稳定速率流出，适合整形。
- 令牌桶：平均速率受控，同时允许一定突发。

### 54. 重试要注意什么？

- 只有临时性错误才重试。
- 操作必须幂等，或有幂等键。
- 使用指数退避加随机抖动。
- 设置次数、单次超时和总时间预算。
- 避免调用链每一层都重试形成重试风暴。

### 55. 分布式事务怎么选？

- 2PC：偏强一致，但性能、阻塞和可用性代价较大。
- TCC：业务自定义 Try/Confirm/Cancel，控制力强但侵入大。
- Saga：一系列本地事务加补偿，适合长流程和可补偿业务。
- Outbox + MQ：适合事件驱动的最终一致性。

第一选择通常是重新划分业务边界，尽量减少跨服务事务，而不是立即选择复杂协议。

### 56. 什么是幂等？

同一个操作执行一次和执行多次，最终业务效果相同。HTTP 方法的协议语义不代表业务实现天然幂等。

常用方案：

- 唯一索引。
- 幂等键及结果复用。
- 状态机和条件更新。
- 去重表。
- Token 防止表单重复提交。

---

## 六、HTTP、网络与安全

### 57. 从输入 URL 到返回页面发生了什么？

**口述版：**

> 浏览器解析 URL，查询 DNS，建立 TCP 或 QUIC 连接；HTTPS 还要完成 TLS 握手。请求经过 CDN、负载均衡和 Nginx，动态请求由 PHP-FPM 或常驻服务处理。应用经过路由、中间件和业务逻辑访问缓存、数据库或 RPC，返回响应。浏览器根据缓存、内容类型和页面资源继续解析渲染。

追问时可以展开 DNS 缓存、TCP 三次握手、TLS 证书校验、Keep-Alive 和 HTTP/2 多路复用。

### 58. GET 和 POST 有什么区别？

不要回答“GET 长度有限、POST 无限”这种绝对结论。

- GET 语义是获取资源，应当安全、幂等，通常可缓存。
- POST 用于提交处理，通常不幂等。
- 参数放 URL 还是请求体是常见形式差异，不是安全边界。
- 长度限制来自浏览器、代理和服务器配置，不是 HTTP 对 GET 的简单固定限制。

### 59. 401 和 403 有什么区别？

- 401：缺少有效身份认证。
- 403：身份已知，但无权访问该资源。

另外：400 表示请求本身有问题，404 表示资源不存在，409 表示状态冲突，422 常用于语义校验失败，429 表示请求过多。

### 60. Cookie、Session、JWT 如何选择？

- Cookie 是浏览器存储和随请求携带数据的机制。
- Session 通常在服务端保存状态，客户端只保存 Session ID，易于撤销，但集群要共享或路由会话。
- JWT 自包含、跨服务验证方便，但即时撤销、续期和权限变更更复杂，载荷只是编码签名，不是自动加密。

不要因为“微服务”就默认 JWT。根据撤销需求、调用边界和安全模型选择。

### 61. HTTPS 如何保证安全？

- 证书和非对称密码机制帮助客户端验证服务器身份并协商密钥。
- 后续数据主要用对称加密保证机密性和完整性。
- 证书链由受信任 CA 体系验证。

HTTPS 不能防止业务漏洞、服务器被入侵或终端本身不可信。

### 62. CSRF 和 XSS 有什么区别？

- CSRF：诱导已登录用户的浏览器携带凭证执行非预期请求。防护：CSRF Token、SameSite Cookie、校验来源、关键操作确认。
- XSS：攻击者注入脚本在用户浏览器执行。防护：按上下文输出编码、模板默认转义、CSP、避免危险 DOM API。

XSS 往往可以绕过 CSRF 防护，因此输入校验不能代替输出编码。

### 63. SQL 注入如何防？

- 使用参数化查询或 ORM 参数绑定。
- 表名、字段名、排序方向不能普通绑定时必须用白名单。
- 数据库账号最小权限。
- 不向用户返回数据库详细错误。

手工转义不是首选，也不能把 WAF 当成根本解决方案。

### 64. SSRF 如何防？

- 只允许必要协议、域名和端口。
- DNS 解析后校验最终 IP，阻止内网、回环和云元数据地址。
- 限制重定向，并对跳转后的目标再次校验。
- 限制响应大小、连接和读取超时。
- 网络层隔离服务的出网权限。

### 65. 密码应该怎样存储？

使用 `password_hash()` 和 `password_verify()` 这类专用密码哈希接口。密码哈希必须是有盐、计算成本可调的慢哈希。不能直接使用 MD5、SHA1 或普通 SHA256 保存密码。

### 66. OAuth 2.0 和 JWT 是什么关系？

OAuth 2.0 是授权框架，定义获取和使用访问令牌的流程；JWT 是一种令牌格式。OAuth 的 Access Token 可以是 JWT，也可以是不透明字符串，二者不是同一个概念。

---

## 七、性能、线上故障与运维

### 67. 接口突然变慢，怎么排查？

**标准流程：**

1. 确认影响范围、开始时间、流量变化和最近发布。
2. 先止损：回滚、降级、限流、切流或扩容。
3. 看 P95/P99、错误率、超时和资源饱和度。
4. 用 Trace 拆分 PHP、SQL、Redis、RPC、MQ 各段耗时。
5. 查看慢查询、锁等待、连接池、缓存命中率、热 Key、队列积压。
6. 根据证据验证假设，修复后持续观察并复盘。

不要一上来就说“加缓存”或“加机器”。

### 68. CPU 很高怎么排查？

- 确定主机、容器和具体进程/线程。
- 看流量是否增加、是否死循环、正则回溯、序列化或计算热点。
- 使用 Profiling/火焰图定位热点函数。
- 检查频繁 GC、日志压缩、加密等 CPU 操作。
- 对比发布和配置变更。

### 69. Load 很高但 CPU 使用率不高，可能是什么？

Load 不只代表正在使用 CPU，也包含处于不可中断等待的任务。可能是磁盘 I/O、NFS、块设备故障、锁等待等。可结合 `vmstat`、`iostat`、`pidstat`、`dmesg` 和进程状态排查。

### 70. 内存持续增长怎么排查？

- 区分正常缓存、内存碎片和真正泄漏。
- 看 RSS、堆内存、请求数和增长趋势。
- 对比代码发布，做对象/堆快照或周期采样。
- 检查静态数组、单例容器、事件监听、闭包、连接和大结果集。
- FPM 可暂用 `pm.max_requests` 回收 worker，但仍需修复根因。

### 71. 502 和 504 有什么区别？

- 502：网关收到无效上游响应，常见于上游未启动、连接被拒绝、进程崩溃、协议或 socket 配置错误。
- 504：网关等待上游响应超时，常见于慢 SQL、下游超时、FPM 队列饱和或程序卡住。

实际排查要结合 Nginx error log、upstream timing、FPM 状态和应用 Trace，状态码只是线索。

### 72. 如何优化接口性能？

1. 先定义目标并测量基线。
2. 定位主要耗时，而不是平均用力。
3. 常见手段：消除 N+1、优化索引、减少 RPC、批处理、缓存、异步化、压缩响应。
4. 检查优化是否转移瓶颈或损害一致性。
5. 用相同数据和流量回归 P95/P99、错误率和资源成本。

### 73. 如何做容量估算？

```text
平均 QPS = 日请求量 ÷ 86400
峰值 QPS = 平均 QPS × 峰值系数
并发量 ≈ QPS × 平均响应时间（秒）
存储量 ≈ 单条大小 × 每日增量 × 保存天数 × 索引及副本系数
```

估算只是初始值，需要压测校准，并为故障、扩容时间和活动峰值预留余量。

### 74. 如何做线上发布？

- CI 完成测试、静态分析、安全扫描和制品构建。
- 制品不可变，同一制品逐环境推广。
- 使用滚动、蓝绿或金丝雀发布。
- 观察错误率、延迟和核心业务指标，触发阈值自动或人工回滚。
- 数据库变更采用 Expand-Contract：先增加兼容结构，再迁移流量和数据，最后删除旧结构。

### 75. 如何设计日志？

- 使用结构化日志。
- 包含时间、级别、服务、环境、trace ID、业务标识、事件和耗时。
- 错误日志记录异常链及必要上下文。
- 不记录密码、完整 Token、密钥等敏感信息。
- 避免在高频循环中打印大量无价值日志。

### 76. Metrics、Logs、Traces 有什么区别？

- Metrics：聚合趋势、仪表盘和告警。
- Logs：记录离散事件和详细上下文。
- Traces：还原请求跨服务调用链和各阶段耗时。

排障时通常从指标发现问题，用 Trace 定位慢在哪一段，再用日志查看具体原因。

---

## 八、系统设计高频问题

### 77. 如何设计秒杀系统？

回答顺序：

1. 澄清库存量、峰值 QPS、一人一单、支付时限和一致性要求。
2. 页面静态化、CDN、资格校验和网关限流。
3. Redis 原子预扣或排队削峰，避免请求直接打数据库。
4. 相同用户/商品请求幂等，数据库唯一约束最终兜底。
5. MQ 异步创建订单，消费端幂等。
6. 支付超时关闭订单并释放库存，设计订单状态机。
7. 处理少卖、超卖、消息丢失/重复、缓存故障和数据对账。
8. 压测、热点隔离、降级开关、监控和应急预案。

面试官更关心失败场景，而不是你列了多少中间件。

### 78. 如何设计订单超时关闭？

常见方案：

- MQ 延迟消息。
- Redis Sorted Set 定时扫描。
- 数据库定时任务兜底。

无论哪种，都要在执行关闭时再次检查订单状态，用条件更新确保只有“待支付”订单能关闭。支付回调与关闭任务并发时依靠状态机和数据库条件更新决定唯一合法结果，并通过对账补偿异常。

### 79. 如何设计接口幂等？

以创建订单为例：

1. 客户端先申请或生成幂等键。
2. 服务端把幂等键和业务范围关联。
3. 首次请求原子占位并执行。
4. 成功后保存响应，重复请求返回同一结果。
5. 执行中的重复请求返回处理中或等待。
6. 数据库唯一约束做最终兜底。

需要定义 Key 生命周期、失败是否允许重试以及相同 Key 不同参数的处理方式。

### 80. 如何设计短链接？

- 长链接生成全局唯一 ID，再进行 Base62 编码，或生成随机短码并处理冲突。
- 短码到长链接存数据库，热点映射放 Redis。
- 跳转接口读多写少，可用 CDN/本地缓存，但要考虑链接禁用与缓存失效。
- 处理过期、防钓鱼、恶意链接、访问统计和热点短链。
- 容量估算要考虑新增量、保存年限、索引、缓存命中率和跳转峰值。

### 81. 系统设计题的万能步骤

1. 功能需求和非功能指标。
2. 流量、并发、带宽、存储估算。
3. API、数据模型和状态机。
4. 高层架构和数据流。
5. 深入最关键的两个问题。
6. 失败、降级、恢复和一致性。
7. 安全、监控、发布、成本和演进。

---

## 九、项目与管理问题

### 82. “介绍一个最有挑战的项目”怎么答？

不要从技术栈列表开始。使用下面结构：

> 项目解决什么业务问题，规模和核心指标是什么；我负责哪一部分；最大挑战是什么；我们比较了哪些方案；我推动了哪些关键动作；如何灰度和验证；最后取得什么量化结果；方案还存在哪些不足。

提前准备 3 分钟简版和 10 分钟展开版。

### 83. “讲一次线上事故”怎么答？

1. 事故影响：时间、用户、业务、数据。
2. 发现方式：监控还是用户反馈。
3. 止损动作：回滚、降级、限流、切流。
4. 根因分析：技术根因与流程根因。
5. 数据恢复和验证。
6. 改进项：代码、测试、发布、监控、预案和负责人。

不甩锅，不隐瞒个人失误。高级工程师要展示止损、协调和系统性改进能力。

### 84. “如何进行技术选型”怎么答？

先列约束和评估维度：

- 业务功能和一致性要求。
- 流量、延迟、容量和可用性。
- 团队经验与招聘成本。
- 社区成熟度、维护状态和生态。
- 运维、迁移、供应商锁定和总体成本。
- 安全、合规和交付时间。

通过小规模 POC 和压测验证关键假设，并保留决策记录。选型不是比较功能列表，而是在约束下做取舍。

### 85. “如何治理技术债”怎么答？

- 建立债务清单，记录影响、风险和偿还成本。
- 按故障风险、开发效率、业务阻塞程度排序。
- 把偿还工作纳入版本计划，而不是等“以后有空”。
- 大重构拆成可验证、可回滚的小步骤。
- 用缺陷率、交付周期、告警和维护成本验证收益。

### 86. “如何做 Code Review”怎么答？

审查顺序：

1. 需求和业务逻辑是否正确。
2. 安全、权限和数据一致性。
3. 事务、幂等、并发和异常路径。
4. 性能和资源释放。
5. 可测试性、可维护性和兼容性。
6. 命名与格式。

反馈应具体说明风险和建议，可自动化的风格问题交给工具。

### 87. “你如何带新人或推动团队改进”怎么答？

给出具体机制和结果，例如：

- 新人学习路径、结对开发、首月任务拆分。
- 设计评审、Code Review 清单、故障复盘模板。
- 建立公共组件前先收集重复问题和使用方需求。
- 用交付周期、缺陷数、故障数等证明改进效果。

不要只回答“耐心沟通、分享知识”。

---

## 十、手写代码常见题

### 88. 用 PHP 实现单例（并说明局限）

```php
final class Config
{
    private static ?self $instance = null;

    private function __construct()
    {
    }

    private function __clone(): void
    {
    }

    public static function instance(): self
    {
        return self::$instance ??= new self();
    }
}
```

面试要补充：单例制造全局状态和隐藏依赖，不利于测试；实际项目优先由 DI 容器管理对象生命周期。常驻进程中单例还可能跨请求污染状态。

### 89. 实现一个简单 LRU Cache，要讲什么？

理想结构是哈希表 + 双向链表：

- 哈希表 O(1) 定位节点。
- 双向链表维护最近使用顺序。
- `get` 后移到头部。
- `put` 满容量时删除尾部。

只用 PHP 数组可以实现功能，但移动元素可能不是严格 O(1)。面试时先讲数据结构和复杂度，再写代码。

### 90. 两个大文件如何找交集？

先问文件大小、内存限制、元素是否唯一及是否允许外部存储。

- 一小一大：小文件建哈希集合，流式扫描大文件。
- 都很大：按相同哈希规则分桶，使相同元素进入同一编号小文件，再逐桶求交集。
- 已排序：双指针流式归并。
- 允许近似预筛：布隆过滤器过滤后再精确确认。

---

## 十一、面试中的危险回答

以下表达容易被继续追问击穿：

- “Redis 是单线程，所以绝对没有并发问题。”
- “加了事务就不会有数据不一致。”
- “用了 MQ 就不会丢消息。”
- “JWT 比 Session 更安全。”
- “用了微服务就能提高性能。”
- “用了索引查询就一定快。”
- “Repeatable Read 完全没有幻读。”
- “分布式锁设置过期时间就绝对安全。”
- “接口慢就增加服务器和缓存。”
- “最终一致性就是过一会儿自然一致。”

更稳妥的表达是说明前提、故障模型、适用范围和补偿机制。

---

## 十二、面试前 30 分钟速查

### 必须能直接回答

- [ ] PHP 数组、COW、垃圾回收、OPcache、FPM。
- [ ] 框架生命周期、容器、ORM N+1。
- [ ] B+Tree、聚簇索引、回表、联合索引、MVCC、锁。
- [ ] Redis 持久化、缓存三大问题、一致性和分布式锁。
- [ ] MQ 可靠投递、重复消费、顺序和积压。
- [ ] 幂等、限流、熔断、降级、分布式事务。
- [ ] HTTPS、Session/JWT、SQL 注入、XSS、CSRF、SSRF。
- [ ] 慢接口、CPU、内存、502/504 的排查路径。
- [ ] 秒杀或订单系统设计。
- [ ] 三个项目故事：难题、事故、团队推动。

### 最后提醒

1. 不知道时先明确边界，不要编造。
2. 先回答问题本身，再扩展相关知识。
3. 不要堆砌中间件，要解释为什么选、出了问题怎么办。
4. 所有性能优化尽量给出优化前后数据。
5. 所有分布式方案都考虑超时、重试、重复、乱序和部分失败。
6. 所有线上故障都先止损，再定位，最后复盘。

